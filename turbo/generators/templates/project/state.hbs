import { State, schema, stringProperty } from "api";

const {{pascalCase name}}Schema = schema({
  name: "{{lowerCase name}}",
  properties: {
    content: stringProperty({
      default: "{{lowerCase name}}"
    }),
  }
});

export type {{pascalCase name}}State = State<typeof {{pascalCase name}}Schema>;

export default {{pascalCase name}}Schema;
